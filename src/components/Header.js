import React from 'react';

const Header = () => (
  <header style={{ backgroundColor: '#282c34', padding: '20px', color: 'white' }}>
    <h1>My React and FastAPI App</h1>
  </header>
);

export default Header;
